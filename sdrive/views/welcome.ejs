<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome</title>
</head>
<body>
    <h1>sDrive</h1>
    <% if (status === "loggedIn") { %>
        <h2>Witaj, <%= user.email %>!</h2>
    <% } else { %>
        <h2>Witaj, gościu!</h2>
        <a href="/login">Zaloguj się</a> lub <a href="/register">Zarejestruj się</a>
    <% } %>
</body>
</html>
